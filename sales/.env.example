# Backend base URL, including /api. Local dev default matches backend/.env's PORT (5050).
VITE_API_BASE_URL=http://localhost:5050/api

# Display name only — no effect on the backend.
VITE_COMPANY_NAME=GO-TO
